= メモ本文エディタ（CodeMirror + AsciiDoc）ロードマップ

このドキュメントは、メモ本文編集を CodeMirror 6 で置き換え、段階的に AsciiDoc 向けの編集体験（ハイライト・補完・WYSIWYG ライト）を足していく際の**作成指針**をまとめたものです。

参考にした設計思想は、Markdown 向け WYSIWYG エディタの解説記事（Decoration + WidgetType で見た目だけプレビューに寄せ、カーソル位置では raw に戻す）です。

* link:https://qiita.com/engchina/items/5246eacbd29daeee7dc2[Qiita: No.1 Markdown Editor の WYSIWYG（engchina）]

kbmemo では **Stimulus**（`MemoBodyEditorController`）+ **Vite** で同様の契約を AsciiDoc に横展開します。CodeMirror 6 自体はフレームワーク非依存のため、React 記事の構成をそのまま流用できます。

== 方針の固定（すべてのフェーズ共通）

[cols="1,3"]
|===
|項目 |内容

|正（ソース・オブ・トゥルース）
|DB / フォームに渡す **AsciiDoc のプレーン文字列**。サーバー側の変換・表示の正は引き続き **Asciidoctor**（`MemoWikiLinks` 等）。

|CodeMirror の役割
|編集体験のみ。ハイライト・補完・WYSIWYG はすべて CM 側の「見せ方」。

|WYSIWYG の型
|ProseMirror 的な「別木構造」にしない。記事どおり **Decoration + Widget + フォーカスで raw に戻す**。

|カーソル契約
|ブロック／インラインからカーソルが**離れている**ときだけ装飾・マーカー非表示。カーソルを**入れたら生ソース**に戻す（ユーザーがソースを信頼できること）。

|デバウンス・保存
|既存の `memo-draft`（Stimulus）との **単一ソース同期**。CM と textarea（および PATCH ペイロード）の二重状態を増やさない。ドラフトは統合 PATCH・直列キュー方針を維持する。

|Turbo
|Stream / partial 差し替え時は `disconnect` で CM を破棄し、textarea 経由で再接続する。
|===

=== AsciiDoc とパーサ戦略

AsciiDoc は Markdown ほど公式な CodeMirror 6 用文法（Lezer 等）が揃っていない。フェーズ進行では次を前提とする。

* 当面は **StreamLanguage / 正規ベース / 最小 HighlightStyle** を段階的に厚くする（`codemirror-asciidoc` 等の利用も可）。
* 将来的に **Lezer grammar / Tree-sitter** を検討する場合は工数を別途見積もる。
* CM 側は**軽量パーズ**、厳密さが必要な確認はサーバー（Asciidoctor）に寄せる**二段**もあり得る。

== フェーズ一覧

各フェーズ末尾の **ゲート** を満たしてから次へ進む。

=== Phase 1 — CodeMirror で textarea を置換（機能パリティ）

* Vite に `@codemirror/state`, `@codemirror/view`, `codemirror`（`basicSetup`）など最小セットを追加。
* `MemoBodyEditorController` で textarea を送信専用（非表示）にし、CM をマウント。
* `EditorView.updateListener` で `textarea.value` と `memo-draft` が読む入力を一致させる。
* Turbo で partial が差し替わったら `disconnect` で破棄・再接続。
* アクセシビリティ: `aria-labelledby` / ラベル連携、エラー時フォーカス。

*ゲート:* 既存ドラフト PATCH・コミットの挙動が導入前と同じ。

*現状（2026-05）:* 実装済み。

=== Phase 2 — AsciiDoc 向け「軽い」言語サポート

* `codemirror-asciidoc`（StreamLanguage）または自前ルールでシンタックス相当を提供。
* 優先順位例: 見出し（`=`）→ リスト → コメント → 強調など。
* Wiki リンク `[[…]]` を **HighlightStyle / Decoration** で見やすく（未解決リンク用 Decoration とも相性がよい）。
* 将来の WYSIWYG と重なっても競合しないよう、Decoration の **レイヤ・マーク prefix** を設計で決めておく。

*ゲート:* 長文でのパフォーマンス、Decoration レイヤ方針の文書化。

*現状（2026-05）:* ハイライト + 完成済み `[[…]]` のマーク装飾まで実装済み。レイヤ prefix の明示設計は Phase 4 前に見直す。

=== Phase 3 — Wiki リンク入力支援

* `@codemirror/autocomplete`: `[[` でタイトル検索 API にデバウンスし候補表示。
* 確定時は `insertCompletionText` に任せず、**明示トランザクション**で `[[full_path/slug]]` 等を挿入（`closeBrackets` による `]]` 自動挿入との二重を避ける）。
* 候補は `policy_scope` と同義の JSON API（`MemoWikiCompletions` / `GET /memos/wiki_completions`）。
* 候補形式: タイトル、同一ディレクトリの slug、フルパス（`MemoWikiLinks` と整合）。

*ゲート:* 線形テキスト上で壊れた `[[` が減り、ユーザーがソースを理解しやすい。

*現状（2026-05）:* 実装済み。候補はタイトル表示・確定時スラッグ挿入。既存 `[[…]]` 内の再選択はリンク全体を置換。

=== Phase 4 —「WYSIWYG ライト」モード（記事の核心）

Qiita 記事の「離れたらプレビュー風／入ったら raw」を AsciiDoc 用に**狭い構文サブセット**から実装する。AsciiDoc 全体を一度に WYSIWYG にしない。

* *ブロック:* 見出し行のみマーカー（`=`）非表示 + タイポグラフィ装飾（Markdown の `#` と同型だが AsciiDoc は `=`）。
* *インライン:* `*bold*` / `_italic_` 等、ごく限定した構文から開始。
* *WidgetType:* タスクリストや簡単な admonition など、製品で許す AsciiDoc 構文に相当するものがあれば同様に検討。
* UX 契約（ソースは信頼できる・カーソル位置で見え方が変わる）をテストまたはこのドキュメントで維持。

*ゲート:* 上記契約を満たすデモ可能な構文範囲が決まっていること。

*現状（2026-05）:* `memo_body_editor/wysiwyg_lite.js` で初期サブセットを実装済み。

* 見出し: 行頭 `=`〜`======`（1〜6 段）。カーソルが行外のとき `=` マーカーを非表示し、見出し用タイポグラフィを適用。
* インライン: `*bold*` / `_italic_` / `` `text` `` / `+text+`（二重 `` / `++` も可。カーソルが範囲内のときはマーカー表示）。
* 除外: フェンスコードブロック（` ``` ` 行）、リンク装飾範囲内のインライン強調。
* `EditorView.atomicRanges` で非表示マーカー上のカーソル挙動を安定化。
* Wiki / AsciiDoc リンク: `wiki_link_wysiwyg.js`（`[[…]]`、`link:`、`<<xref>>`、URL）。スラッグ解決時はタイトル表示、解決済みはクリックで `/memos/:id` へ。

*Phase 4 で意図的に残していたもの:* リスト行 → **Phase 5a**、admonition・ブロック Widget → **5b 以降**（下記）。

=== Phase 5 — 複合ブロック（コスト・難易度が跳ねるゾーン）

Phase 4 までの Decoration ベース WYSIWYG を、**ブロック単位**に広げるフェーズ。
いきなり Widget やネストエディタに入らず、**5a → 5b → 5c** と段階を分ける（難易度の跳ね上がりを抑える）。

共通契約（Phase 4 と同じ）:

* 正は DB の AsciiDoc プレーン文字列。CM は見せ方のみ。
* **フォーカスなし**、または**カーソルがブロック外** → プレビュー風。
* **カーソルがブロック内** → 生 AsciiDoc。
* 厳密な構文検証は Asciidoctor（表示・コミット後）に寄せ、CM は軽量パース。

==== 着手前に固定する意思決定

[cols="2,3"]
|===
|論点 |メモ

|許す AsciiDoc サブセット
|*決定（2026-05）:* Phase 4 + 5a–5d + 5f–5g の第1段を正式範囲とする。一覧は「Phase 5 完了 — 正式サポート範囲」表を参照。

|テーブル
|*決定（2026-05）:* 当面 **ソース編集のみ**（5d 第1段）。セル Widget は Phase 6 以降。

|Mermaid / 数式
|*決定（2026-05）:* 図は `diagram::` + 専用編集ページ、数式は `stem:` / `latexmath:`。表示は KaTeX / Kroki。CM 内プレビューは *2026-05 実装*（`diagram_wysiwyg` / `math_wysiwyg`）。

|Widget の導入基準
|Decoration で足りるもの（リスト行・admonition 見出し）は Widget にしない。折りたたみ・ネスト CM が必要なときだけ Widget。
|===

==== Phase 5a — リスト行 WYSIWYG ライト（推奨: 最初の着手）

*対象（案）:* 箇条書き・番号付きの行頭マーカー。例: `*` / `-` / `.` / `1.` など（AsciiDoc の list 行。入れ子は後続でも可）。
*方式:* `wysiwyg_lite.js` の延長。行単位 `Decoration.line` + マーカー部分の `Decoration.replace`。
*カーソル:* その行に入ったら raw（Phase 4 見出しと同型）。
*ゲート:* フェンス内・リンク範囲と競合しない。長文（数千行）で装飾再計算が実用速度。

*現状（2026-05）:* 実装済み（`wysiwyg_lite.js`）。`*` / `-` / `.` / `1.` 等の行頭リスト。マーカー非表示・段階インデント・カーソル行は raw。

==== Phase 5b — Admonition（NOTE / TIP / WARNING 等）

*対象（案）:* 行頭 `NOTE:`、`TIP:`、`IMPORTANT:`、`WARNING:`、`CAUTION:` と続く本文ブロック。
*方式（第1段）:* ラベル行のマーカー非表示 + ブロック用 line class（左ボーダー・背景）。ラベル行のみバッジ Widget。
*方式（第2段・任意）:* 複数行ブロック全体を折りたたみ可能な `WidgetType`。
*ブロック境界（軽量パーサ）:* フェンス外で、ラベル行の次行から *空行*・*次の admonition ラベル*・*見出し（`=`）*・*フェンス（` ``` `）*・*ブロック区切り（`----` 等）* の手前までを同一ブロックとする（Asciidoctor の段落区切りに合わせる）。空行を挟んだ段落はブロック外。
*カーソル:* ブロック内のいずれかの行にカーソルがある間はブロック全体を raw。
*ゲート:* フェンス内は対象外。リンク範囲内インラインは Phase 4 どおり。

*現状（2026-05）:* 第1段実装済み（`admonition_syntax.js` / `admonition_wysiwyg.js` / `wysiwyg_lite.js`）。

==== Phase 5c — フェンスコード / listing

*対象:* AsciiDoc ソースブロック（`.title` + `[source,lang]` + `----` … `----`）、listing / literal（`----` / `....`）。補助として Markdown `` ``` `` フェンスも認識。
*方式（段階的）:*
** 第1段: ブロック背景・等幅・言語ラベル表示（Decoration / line mark）。編集は全文エディタのまま。
** 第2段: 折りたたみ（`foldGutter` または ViewPlugin）。
** 第3段: ブロック内ネスト CM（別パネルハイライト）— コスト大、必要時のみ。
*カーソル:* ブロック内のいずれかの行にカーソルがある間はブロック全体を raw。
*既存:* フェンス内はインライン WYSIWYG・admonition パース対象外。
*ゲート:* lazy 読み込み（言語ハイライト用 grammar はブロック表示時のみ）。巨大メモでスクロール・再描画を計測。

*現状（2026-05）:* 第1段実装済み。AsciiDoc ソースブロックのタイトル・`[source,lang]`・区切り行を WYSIWYG 化（[syntax-quick-reference](https://docs.asciidoctor.org/asciidoc/latest/syntax-quick-reference/) 準拠）。

==== Phase 5d — テーブル

*対象:* 製品で許す表記のみ（例: pipe 表 `|===` のみ、など要決定）。
*方式:* セル単位 `WidgetType` は **最重**。ソース編集を維持しつつ、ヘッダー行だけ WYSIWYG する等の縮小案も可。
*ゲート:* 表記バリエーション一覧 + 「編集は常にソース」か「セル UI あり」かの製品判断。

*現状（2026-05）:* 第1段実装済み。`|===` pipe 表。1行1セル形式は `[cols]` と空行で論理行にグループ化。表装飾は **StateField + block replace**（ViewPlugin 不可のため `table_wysiwyg_field.js`）。カーソルがブロック内のときはブロック全体 raw。

==== Phase 5f — 画像（メモアセット）

*保存:* Git 作業ツリー上の `{slug}.assets/`（`.adoc` と同階層）。本文は `image::filename[]`、表示時 `imagesdir` を `/memos/:id/assets/` に設定。
*API:* `POST /memos/:id/assets`（アップロード）、`GET /memos/:id/assets/:filename`（配信）。コミット時にアセットも `git add`。
*現状（2026-05）:* 基盤実装済み（PNG/JPEG/GIF/WebP/SVG、10MB）。ファイル名は `MemoAssetFilename` で NFC 正規化し、日本語可。SVG は `MemoSvgSanitizer` で script 等を除去。挿入: ファイル選択・D&D・クリップボード貼り付け（スクリーンショット等、プレーンテキスト同時の場合は通常貼り付け）。CM 内プレビューは `image_wysiwyg.js`。

*TODO — 未参照アセット:* 本文から `image::` / `image:` 行を削除しても `{slug}.assets/` 内のファイルは削除されず、メモ保存時もフォルダ内の全ファイルが `git add` される。直接 URL でも配信され続ける。方針未決定。候補: 保存時に本文をスキャンして未参照ファイルを削除してからコミット、未使用一覧＋手動削除 UI、`DELETE` API。実装前に「自動削除 vs 手動」「Git 履歴に残すか」を決める。

==== Phase 5e — ダイアグラム（Mermaid / PlantUML）

*記法:* `diagram::name.mmd[]` / `diagram::name.puml[]`（`diagrams/` 配下にソース、同ベース名の `.svg` を Kroki 生成）。
*編集:* メモ CM には組み込まない。`/memos/:id/diagrams/:key/edit` の専用ページでソース編集 → 保存時に Kroki → `.svg` を Git 作業ツリーへ。
*表示:* `MemoDiagramMacro` が `diagram::` を `image::diagrams/*.svg[opts=interactive]` に展開（`memo_html` / 将来 CM WYSIWYG）。
*インフラ:* `KROKI_URL`（既定 `http://localhost:8001`）。`docker compose -f docker-compose.kroki.yml up -d`。
*対応種別（2026-05）:* Mermaid（`.mmd` / `.mermaid`）、PlantUML（`.puml` / `.plantuml` / `.uml`）。
*CM（2026-05）:* `diagram::` 行の SVG プレビュー（`<object>`）、「編集」リンク（専用ページ）。カーソル行は生 AsciiDoc。
*未着手:* CM 内クリックでソース切替。

==== Phase 5g — 数式（STEM / KaTeX）

*記法:* AsciiDoc 標準の `stem:[...]` / `latexmath:[...]`、ブロックは `[stem]` + `++++` パススルー（`:stem: latexmath`）。
*表示:* `memo_html` で asciidoctor-katex によりサーバー側レンダリング。KaTeX CSS は Vite 経由。
*CM（2026-05）:* `stem:` / `latexmath:` インライン、`[stem]` + `++++` ブロックの KaTeX プレビュー。カーソル行・ブロック内は生 AsciiDoc。

==== Phase 5 全体のゲート

* 複雑 Widget は **lazy 読み込み**（初期表示・スクロール_into_view 時）。→ *2026-05:* `viewport_lazy.js` で WYSIWYG 装飾を可視行±バッファに限定。表示画面の `<img>` は `loading=lazy`。
* **巨大メモ**（例: 10k 行級）での装飾ビルド時間・メモリを一度計測し、上限または打ち切り方針を決める。→ *Phase 5 締め:* 下記「性能ゲート」を参照（手動確認 + 任意計測）。
* 各サブフェーズ（5a〜）ごとに PR を分け、レビュー可能な単位を保つ。→ *達成*（feature ブランチ上でサブフェーズ単位のコミット）。

==== Phase 5 完了 — 正式サポート範囲（2026-05）

Phase 5 は **各サブフェーズの第1段**を揃えた状態で完了とする。編集 CM はソースのまま、**表示・コミット後プレビューは Asciidoctor**（+ 図・数式の前処理）を正とする。

[cols="1,2,2"]
|===
|領域 |編集（CM） |表示（`memo_html`）

|Phase 4 継承
|見出し・インライン強調・Wiki / リンク
|同上 + `MemoWikiLinks`

|5a リスト
|行頭 `*` / `-` / `.` / `1.` 等の WYSIWYG ライト
|Asciidoctor リスト

|5b Admonition
|ラベル行バッジ + ブロック装飾（折りたたみなし）
|`NOTE:` / `TIP:` / `WARNING:` 等

|5c コード
|ソースブロック・listing の枠・言語ラベル（折りたたみなし）
|Asciidoctor ハイライト

|5d テーブル
|`|===` pipe 表の枠装飾（**セル UI なし・ソース編集**）
|表ブロック

|5f 画像
|D&D・クリップボード貼付・`image::` 挿入。CM 内 `image_wysiwyg` プレビュー
|`imagesdir` + アセット URL

|5e ダイアグラム
|専用ページで編集 + CM 内 SVG プレビュー（`diagram_wysiwyg`）
|`diagram::` → Kroki 生成 SVG（`object` 表示）

|5g 数式
|CM 内 KaTeX プレビュー（`math_wysiwyg`）
|asciidoctor-katex（`:stem: latexmath`）
|===

==== Phase 5 締め — スモークチェックリスト

マージ前に **1 本のメモ**で以下を通す（表示画面 + 編集画面）。

. リスト入れ子・番号付きリスト
. `NOTE:` / `WARNING:` ブロック（複数行）
. `[source,ruby]` + `----` フェンス、行番号なしで編集可能
. `|===` 表（ヘッダー + 複数行）
. `image::` 画像（アップロード or 既存アセット）
. `diagram::foo.mmd[]`（Kroki 起動済み・SVG 再保存済み）
. `stem:[E=mc^2]` と `latexmath:[\int x dx]`、ブロック `[stem]` + `++++`
. Wiki リンク `[[slug]]` の解決・クリック

==== Phase 5 締め — 性能ゲート（手動）

* *目安:* 約 3,000 行の AsciiDoc を開き、初回表示〜スクロール・行移動で「実用できる」こと（体感 1 秒未満の入力遅延が続くこと）。
* *未達のとき:* 装飾のスコープ縮小、行単位の再計算デバウンス強化、画像/表の lazy を検討（Issue 化）。
* *任意:* DevTools Performance で長い `update` を記録し、遅い ViewPlugin / StateField 名をメモに残す。

==== Phase 5 以降に回すもの（Phase 5 完了の必須条件ではない）

[cols="2,3"]
|===
|項目 |メモ

|5b 第2段
|admonition 折りたたみ Widget

|5c 第2–3段
|コードブロック折りたたみ・ネスト CM

|5d セル Widget
|表のセル編集 UI

|5e CM クリック編集
|CM 内クリックでソース切替（プレビュー・編集リンクは実装済）

|5g CM 拡張
|追加 AsciiDoc 数式記法のサポート拡大

|5f 未参照アセット
|本文から消した画像・図ファイルの削除方針（自動 / 手動 UI）

|lazy 読み込み
|*実装済（2026-05）:* CM WYSIWYG の viewport 外スキップ + 表示 `<img>` lazy

|DnD ドラフト + Turbo Stream
|サイドバー連動（リスク節・任意）
|===

=== Phase 6 — Turbo・マルチタブ・将来拡張

==== Phase 6a — マルチタブ・Turbo Stream（2026-05 着手）

*マルチタブ（同一ブラウザ）:*

* `BroadcastChannel`（`kbmemo.memo.{id}`）でドラフト保存をタブ間通知。
* 受信タブで本文が未保存変更なし → textarea / CodeMirror に自動反映（`change` 経由）。
* 未保存変更あり → 警告バナー。「最新の本文を読み込む」で明示的に上書き。
* 保存側タブは `notifyRemoteDraftSaved` で本文スナップショットを送信。

*Turbo Stream（表示ペイン）:*

* `turbo_stream_from` による `show_content` 差し替え時、`#memos_editor_scroll` のスクロール位置を復元（`application.js`）。
* 編集フォームはドラフト応答で `title` / `slug` / サイドバーのみ Stream 更新（本文 CM は差し替えない）。

*エディタ状態（方針）:*

* CM の選択・undo は Stream では触らない（編集 partial を Stream しない）。
* 他タブ本文の取り込みは全文差し替え（`memo_body_editor_controller` の `_onTextareaExternalChange`）。
* 競合時はユーザー確認（バナー）を優先し、黙って上書きしない。

==== Phase 6b — WYSIWYG トグル（2026-05）

* `Compartment` で WYSIWYG 拡張パック（`wysiwyg_lite` / 画像 / 表 / リンク）をオンオフ。
* ツールバー「ソース表示」⇔「プレビュー風」。設定は `localStorage`（`kbmemo_memo_editor_wysiwyg`、既定オン）。
* オフ時もシンタックスハイライト・Wiki 補完・リスト継続は有効。

*未着手:* E2E（Playwright 等）。別端末間の競合は Phase 6 範囲外。

== リスクと意思決定（早めに固定）

[cols="2,3"]
|===
|論点 |推奨

|AsciiDoc 全体を一度に WYSIWYG にするか
|**しない**。Phase 4 は構文サブセットを決めて増やす。

|CM 側パーサの厳密さ
|編集用は軽量でよい。公開プレビュー・コミット後表示は Asciidoctor を正とする。

|React 記事との差
|kbmemo は Stimulus。CM API はそのまま利用可能。

|DnD 等からのドラフト
|*決定（2026-05）:* `memo-draft` は `Accept: text/vnd.turbo-stream.html` でサイドバー同期済み。DnD からのドラフトも同経路で可。
|===

== 推奨マイルストーン順（まとめ）

. CM で textarea と同等保存（Phase 1）
. 軽ハイライト + Wiki Completion（Phase 2–3）
. Decoration ベースの WYSIWYG-lite（見出し → インライン → リンク）（Phase 4）
. ブロック拡張: リスト（5a）→ admonition（5b）→ コード（5c）→ 表（5d）→ 画像（5f）→ 図（5e）→ 数式（5g）（Phase 5 第1段完了）
. Turbo / マルチタブ / Compartment 切替（Phase 6）

記事が示す設計哲学（**ソース不変・カーソル駆動の raw 復帰**）を保ったまま、AsciiDoc 向けに段階的に寄せる。

== Wiki リンク用スラッグ（全体一意）

* DB の `memos.slug` は `{stem}-{memo_id}` 形式（例: `first-memo-42`）。**アプリ全体で一意**。
* Wiki リンクの推奨表記: `[[first-memo-42]]`（ディレクトリ移動の影響を受けない）。
* レガシー: `[[stem]]`（stem が scope 内で一意のとき）、`[[full_path/slug-{id}]]`。
* Git ファイル名は `{global_slug}.adoc`（`MemoRepository`）。旧 `{slug}-{id}.adoc` から移行済み。

== 関連コード

* `app/javascript/controllers/memo_body_editor_controller.js` — CM マウント・textarea 同期
* `app/javascript/adoc_editor/kbmemo_asciidoc_extensions.js` — 言語ハイライト・`[[…]]` 装飾
* `packages/adoc-kbmemo/src/wiki_completion.js` — Wiki オートコンプリート
* `packages/adoc-kbmemo/src/viewport_lazy.js` — 可視行範囲の追跡と WYSIWYG 装飾の lazy スコープ
* `packages/adoc-kbmemo/src/diagram_wysiwyg.js` — `diagram::` SVG プレビュー（5e CM）
* `packages/adoc-kbmemo/src/math_wysiwyg.js` — stem / latexmath KaTeX プレビュー（5g CM）
* `packages/adoc-kbmemo/src/code_block_syntax.js` — フェンス / listing / literal ブロック走査
* `packages/adoc-kbmemo/src/wiki_link_wysiwyg.js` — Wiki / AsciiDoc リンク WYSIWYG
* `packages/adoc-wysiwyg/wysiwyg.js` — ブロック単位 WYSIWYG エディタ
* `packages/adoc-preview/live_preview.js` — ライブ HTML プレビュー
* `packages/adoc-kbmemo/hostConfig.js` — CSRF・URL・スクロールルート等の HostConfig
* `npm run build:packages` — workspace パッケージの ESM ビルド（`tsup` → `packages/*/dist/`、publish 用）
* `npm run test:packages` — Vitest（`packages/adoc-codemirror/test/` 等）
* `npm run pack:packages` — ビルド後に `npm pack --dry-run` で tarball 内容を検証
* `npm run publish:packages -- --dry-run` — publish 前チェック（本番は `NPM_PUBLISH_REGISTRY` を設定）
* `npm run verify:consumer` — tarball を別ディレクトリに `npm install` して `@kbmemo/adoc-editor` を smoke  import
* `examples/adoc-editor-demo/` — ソース + ライブプレビューの最小デモ（`npm run dev --workspace adoc-editor-demo`）
* `app/services/memo_wiki_link_labels.rb` — リンク WYSIWYG 用ラベル API（`GET /memos/wiki_link_labels`）
* `app/javascript/controllers/memo_draft_controller.js` — ドラフト保存（正の同期先）
* `app/services/memo_wiki_completions.rb` — 補完候補 API
* `app/services/memo_wiki_links.rb` — 表示時の Wiki リンク解決
* `packages/adoc-kbmemo/src/table_syntax.js` — pipe 表ブロック境界（5d）
* `packages/adoc-kbmemo/src/image_syntax.js` — 画像マクロ（5f）
* `app/services/memo_assets.rb` / `memo_asset_path.rb` — アセット保存・配信
* `app/services/memo_diagram*.rb` / `memo_diagrams_controller.rb` — ダイアグラム（5e）
* `app/services/memo_diagram_macro.rb` — `diagram::` 展開
* `config/initializers/kroki.rb` / `docker-compose.kroki.yml` — Kroki
* `config/initializers/asciidoctor.rb` — STEM / KaTeX（5g）
* `app/javascript/controllers/memo_draft_controller.js` — ドラフト・マルチタブ `BroadcastChannel`
* `app/frontend/entrypoints/application.js` — Turbo Stream 後のスクロール復元
* `app/controllers/memos_controller.rb` — `wiki_completions`, `draft`

== npm パッケージ — 他プロジェクトからの利用

[source,javascript]
----
import { EditorState } from '@codemirror/state'
import { EditorView } from '@codemirror/view'
import { basicSetup } from 'codemirror'
import {
  asciidocHighlight,
  configureVanillaHost,
  createLivePreview,
  createWysiwygEditor,
  createWysiwygSourceExtensions,
} from '@kbmemo/adoc-editor'
import 'highlight.js/styles/github.min.css'
import '@kbmemo/adoc-preview/preview_hljs.css'
import '@kbmemo/adoc-wysiwyg/wysiwyg.css'

configureVanillaHost({ memoBase: '/memos' })
----

* KBMemo 本体向け拡張は `@kbmemo/adoc-kbmemo` の `configureKbmemoHost` / `createKbmemoWysiwygSourceExtensions`
* Rails + Stimulus 接続は site 側アダプタ（`memo_body_editor_controller.js`, `adoc_editor/mount.js`）に残す
* レジストリ設定は `.npmrc.example` を参照
